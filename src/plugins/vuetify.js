import Vue from 'vue';
import Vuetify from 'vuetify';
import { 
    VTextField,
    VMain
} from 'vuetify/lib';

Vue.use(Vuetify, {
    components: {
        VTextField,
        VMain
    }
});

export default new Vuetify({
});
